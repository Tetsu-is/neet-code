#include <bits/stdc++.h>
using namespace std;

class Solution {
    public:
        vector<vector<string>> groupAnagrams(vector<string>& strs) {
            // はじめの要素の文字マップを作る
            // 同じマップになるものをカテゴリとして保存する
            // 同じものがなくなったら次のターゲットにいく(このときカテゴライズ済みであればスキップ)
            vector<vector<string>> categories = {};
            
        }
    };